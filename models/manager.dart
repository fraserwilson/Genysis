class Manager{

  final String name;
  final String grade; // make into its own model
  final String stats; // make into its own model
  final String team; //use team model here

  Manager(this.name,this.grade,this.stats,this.team);
  

}