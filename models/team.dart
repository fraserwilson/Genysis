class Team{

  final String name;
  final String players; // use player model rather than normal string
  final String rating; //based on player scores
  final String record; // seperate into wins and losses, do calc to determine

  Team(this.name,this.players,this.rating,this.record);
  

}